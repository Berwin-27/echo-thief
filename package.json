{
  "name": "echo-thief",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "playwright": "^1.60.0",
    "vite": "^5.4.0"
  }
}
